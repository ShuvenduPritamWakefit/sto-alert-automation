# Empty stub module.
# The notebook runs "import sql_explorer as exp" (an internal helper that only
# exists on your laptop) but never actually calls it. This file makes that
# import succeed on GitHub Actions without changing the notebook at all.
